class GFG {

    public static Long product(Long arr[], Long mod, int n) {
        long product = 1;

        for (int i = 0; i < n; i++) {
            product = (product * arr[i]) % mod;
        }

        return product;
    }
}
